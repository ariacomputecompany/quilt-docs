---
title: "Operations"
description: "Track accepted Quilt lifecycle work instead of issuing duplicate mutations."
sidebarTitle: "Operations"
---

Async lifecycle routes typically return an operation handle.

Treat that handle as part of the contract, not as optional bookkeeping.

## Operation lookup

```text
GET /api/operations/<operation_id>
```

## Event stream

```text
GET /api/events
```

`GET /api/events` is an SSE stream returning `text/event-stream`, not a JSON array.

## Accepted response

```json
{
  "success": true,
  "operation_id": "op_123",
  "status_url": "/api/operations/op_123"
}
```

Observed statuses:

- `accepted`
- `queued`
- `running`
- `succeeded`
- `failed`
- `cancelled`
- `timed_out`

## What operations are for

Operations show up when Quilt accepts work that may take time, including create, stop, resume, snapshot, fork, and delete flows.

## Rule of thumb

If a mutation returned an operation, Quilt already has your request. Prefer tracking that handle over retrying blindly.
